<%- include('../../../shared/includes/all/head-all', {
	svgIncludes: [
		// ['../svg/logo/logo-icon-svg', {navLogoPrimaryFillColor: '#0d6efd'}],
		['../svg/logo/ny-logo-icon-svg', {navLogoPrimaryFillColor: '#0d6efd'}],
		'../svg/icons/bootstrap-icons/bi-arrow-left',
		'../svg/icons/bootstrap-icons/bi-exclamation-triangle-fill',
		'../svg/icons/bootstrap-icons/bi-check-circle-fill',
	],
})
%>
<div id="contentWrapper" class="flex-fill d-flex align-items-center justify-content-center bg-primary bg-gradient"><% /* opening contentWrapper */ %>
	<main><% /* opening main tag */ %>

		<div class="container-lg containerExtraNarrow my-1"> <% /* "my-4" indsættes dynamisk hvis indhold (inkl. 4px margin i top og bund) af .container-lg er mindre end viewport (se "centered-content-form-pages.js") */ %>
			<section class="border rounded-3 bg-body-tertiary shadow-lg mb-4">
				<div class="row p-3 p-sm-4 px-md-5">
					<div class="col-12 text-center mb-3">
						<% const logoFileType = 0;
						if (logoFileType === 1) { %>
							<img id="logoImg" src="/images/shared/header/logo.png" alt="logo">
						<% } else if (logoFileType === 0) { %>
							<% /* det er vigtigt at viewBox="..." er der ellers vil svg optage mere plads end den burde */ %>
							<% if (0) { %>
								<svg id="logoImg" viewBox="0 0 39.166599 40.000042"><use xlink:href="#logo-icon"></use></svg>
							<% } else { %>
								<svg id="logoImg" viewBox="0 0 31.167688 22.52528"><use xlink:href="#ny-logo-icon"></use></svg>
							<% } %>
						<% } %>
					</div>